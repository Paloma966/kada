# Kada 环境变量示例：复制为 .env 并填写真实值后 `docker compose up -d`
#
# 生成强随机 JWT 密钥：openssl rand -hex 32
JWT_SECRET=
#
# 数据库密码（生产环境必须修改，勿使用 kada123）
POSTGRES_PASSWORD=
#
# 阿里云短信（可选，未配置时验证码仅开发模式打印）
SMS_ACCESS_KEY_ID=
SMS_ACCESS_KEY_SECRET=
SMS_SIGN_NAME=
SMS_TEMPLATE_CODE=
