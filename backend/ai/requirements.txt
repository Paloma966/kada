# Kada AI 服务依赖（backend/ai）
# 版本锁定自本地开发环境（Python 3.11），保证部署/协作环境可复现。

# Web 框架与服务器
fastapi==0.141.1
uvicorn==0.52.4
watchfiles==1.2.0        # uvicorn --reload 热重载依赖

# 配置
pydantic-settings==2.15.0

# 数据库：会话表走 SQLModel + asyncpg；向量库走 psycopg
sqlmodel==0.0.42
asyncpg==0.31.0
psycopg[binary]==3.3.5
redis==4.6.0

# LangChain 生态（对话 / 工具 / 向量 / MCP）
langchain==1.4.0
langchain-core==1.6.2
langchain-openai==1.6.2
langchain-community==0.4.2
langchain-postgres==0.0.18
langchain-text-splitters==1.1.2
langchain-mcp-adapters==0.3.2

# MCP 子服务与回调 Go 后端
mcp==1.24.0
httpx==0.28.1
